export interface Note {
    title: string,
    description: string,
    link: string | null,
    id: string 
}

export interface NoteDoc {
    title: string,
    description: string,
    link: string
}